{
  "id": "idempotent-webhook-consumer",
  "title": "Idempotent Webhook Consumer",
  "category": "backend",
  "difficulty": "medium",
  "description": "Consume webhook events safely with idempotent deduplication by event_id.\n\n### Objective\nImplement the FastAPI service so the replayed evaluator requests pass exactly as specified.\n\n### Implement These Routes\n- `POST /webhook/events`\n\n### Request Replay (Executed In Order)\n1. `POST /webhook/events`\n   Request body: `{\"event_id\":\"evt-1\",\"type\":\"user.created\"}`\n   Expected status: `202`\n   Expected body: `{\"processed\":true,\"duplicate\":false}`\n\n2. `POST /webhook/events`\n   Request body: `{\"event_id\":\"evt-1\",\"type\":\"user.created\"}`\n   Expected status: `200`\n   Expected body: `{\"processed\":true,\"duplicate\":true}`\n\n3. `POST /webhook/events`\n   Request body: `{\"event_id\":\"evt-2\",\"type\":\"user.updated\"}`\n   Expected status: `202`\n   Expected body: `{\"processed\":true,\"duplicate\":false}`\n\n### Candidate Expectations\n- Complete the TODOs in starter code and keep behavior deterministic across sequential requests.\n- Keep state in memory for this challenge; do not use SQL or external databases.\n- Return exact status codes and JSON response shapes expected by the replay tests.\n- Handle invalid or missing input defensively with stable JSON errors instead of crashes.\n\n### Starter Files You Can Edit\n- `app/main.py`\n\n### Done Criteria\n- All replayed request tests pass in order.\n- API behavior matches the required contracts exactly.",
  "constraints": "- Python 3.11+\n- No SQL or external database usage\n- Deterministic outputs for graded requests",
  "hints": [
    "Store processed event ids in a set.",
    "Do not process duplicate side effects twice."
  ],
  "tags": [
    "backend",
    "python",
    "fastapi",
    "webhook-consumers",
    "idempotency"
  ],
  "starter_files": [
    {
      "path": "app/main.py",
      "content": "from fastapi import FastAPI, HTTPException\nfrom typing import Any, Dict\n\napp = FastAPI(title=\"Idempotent Webhook Consumer\")\n\n# In-memory state for this challenge.\nSTATE: Dict[str, Any] = {}\n\n# TODO: Implement all required endpoints from the prompt.\n\n@app.post(\"/webhook/events\")\nasync def idempotent_webhook_consumer_post_webhook_events(payload: Dict[str, Any]):\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n",
      "language": "python",
      "readonly": false
    }
  ],
  "test_config": {
    "language": "python",
    "test_requests": [
      {
        "method": "POST",
        "path": "/webhook/events",
        "expected_status": 202,
        "expected_body": {
          "processed": true,
          "duplicate": false
        },
        "body": {
          "event_id": "evt-1",
          "type": "user.created"
        }
      },
      {
        "method": "POST",
        "path": "/webhook/events",
        "expected_status": 200,
        "expected_body": {
          "processed": true,
          "duplicate": true
        },
        "body": {
          "event_id": "evt-1",
          "type": "user.created"
        }
      },
      {
        "method": "POST",
        "path": "/webhook/events",
        "expected_status": 202,
        "expected_body": {
          "processed": true,
          "duplicate": false
        },
        "body": {
          "event_id": "evt-2",
          "type": "user.updated"
        }
      }
    ]
  },
  "solution_files": [],
  "sort_order": 30
}
