{
  "id": "reconnect-missed-events",
  "title": "Reconnect Missed Events",
  "category": "backend",
  "difficulty": "hard",
  "description": "Allow reconnecting clients to fetch missed events since last seen cursor.\n\n### Objective\nImplement the FastAPI service so the replayed evaluator requests pass exactly as specified.\n\n### Implement These Routes\n- `POST /events/publish`\n- `GET /events/missed?last_seen=evt-1`\n\n### Request Replay (Executed In Order)\n1. `POST /events/publish`\n   Request body: `{\"topic\":\"system\",\"message\":\"a\"}`\n   Expected status: `200`\n   Expected body: `{\"published\":true,\"event_id\":\"evt-1\"}`\n\n2. `POST /events/publish`\n   Request body: `{\"topic\":\"system\",\"message\":\"b\"}`\n   Expected status: `200`\n   Expected body: `{\"published\":true,\"event_id\":\"evt-2\"}`\n\n3. `GET /events/missed?last_seen=evt-1`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"events\":[{\"id\":\"evt-2\",\"message\":\"b\"}]}`\n\n### Candidate Expectations\n- Complete the TODOs in starter code and keep behavior deterministic across sequential requests.\n- Keep state in memory for this challenge; do not use SQL or external databases.\n- Return exact status codes and JSON response shapes expected by the replay tests.\n- Handle invalid or missing input defensively with stable JSON errors instead of crashes.\n\n### Starter Files You Can Edit\n- `app/main.py`\n\n### Done Criteria\n- All replayed request tests pass in order.\n- API behavior matches the required contracts exactly.",
  "constraints": "- Python 3.11+\n- No SQL or external database usage\n- Deterministic outputs for graded requests",
  "hints": [
    "Maintain a globally ordered event list.",
    "Find last_seen index before slicing."
  ],
  "tags": [
    "backend",
    "python",
    "fastapi",
    "reconnect",
    "event-streams"
  ],
  "starter_files": [
    {
      "path": "app/main.py",
      "content": "from fastapi import FastAPI, HTTPException\nfrom typing import Any, Dict\n\napp = FastAPI(title=\"Reconnect Missed Events\")\n\n# In-memory state for this challenge.\nSTATE: Dict[str, Any] = {}\n\n# TODO: Implement all required endpoints from the prompt.\n\n@app.post(\"/events/publish\")\nasync def reconnect_missed_events_post_events_publish(payload: Dict[str, Any]):\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n\n@app.get(\"/events/missed\")\nasync def reconnect_missed_events_get_events_missed():\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n",
      "language": "python",
      "readonly": false
    }
  ],
  "test_config": {
    "language": "python",
    "test_requests": [
      {
        "method": "POST",
        "path": "/events/publish",
        "expected_status": 200,
        "expected_body": {
          "published": true,
          "event_id": "evt-1"
        },
        "body": {
          "topic": "system",
          "message": "a"
        }
      },
      {
        "method": "POST",
        "path": "/events/publish",
        "expected_status": 200,
        "expected_body": {
          "published": true,
          "event_id": "evt-2"
        },
        "body": {
          "topic": "system",
          "message": "b"
        }
      },
      {
        "method": "GET",
        "path": "/events/missed?last_seen=evt-1",
        "expected_status": 200,
        "expected_body": {
          "events": [
            {
              "id": "evt-2",
              "message": "b"
            }
          ]
        }
      }
    ]
  },
  "solution_files": [],
  "sort_order": 50
}
