{
  "id": "origin-allowlist-policy",
  "title": "Origin Allowlist Policy",
  "category": "backend",
  "difficulty": "medium",
  "description": "Apply a strict origin allowlist policy for cross-origin style access decisions.\n\n### Objective\nImplement the FastAPI service so the replayed evaluator requests pass exactly as specified.\n\n### Implement These Routes\n- `GET /public?origin=https://app.local`\n- `GET /public?origin=https://evil.example`\n- `GET /public?origin=https://admin.local`\n\n### Request Replay (Executed In Order)\n1. `GET /public?origin=https://app.local`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"allowed\":true}`\n\n2. `GET /public?origin=https://evil.example`\n   Request body: `(not provided)`\n   Expected status: `403`\n   Expected body: `{\"error\":\"origin_blocked\"}`\n\n3. `GET /public?origin=https://admin.local`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"allowed\":true}`\n\n### Candidate Expectations\n- Complete the TODOs in starter code and keep behavior deterministic across sequential requests.\n- Keep state in memory for this challenge; do not use SQL or external databases.\n- Return exact status codes and JSON response shapes expected by the replay tests.\n- Handle invalid or missing input defensively with stable JSON errors instead of crashes.\n\n### Starter Files You Can Edit\n- `app/main.py`\n\n### Done Criteria\n- All replayed request tests pass in order.\n- API behavior matches the required contracts exactly.",
  "constraints": "- Python 3.11+\n- No SQL or external database usage\n- Deterministic outputs for graded requests",
  "hints": [
    "Use a set for allowed origins.",
    "Normalize incoming origin before compare."
  ],
  "tags": [
    "backend",
    "python",
    "fastapi",
    "policy",
    "security"
  ],
  "starter_files": [
    {
      "path": "app/main.py",
      "content": "from fastapi import FastAPI, HTTPException\nfrom typing import Any, Dict\n\napp = FastAPI(title=\"Origin Allowlist Policy\")\n\n# In-memory state for this challenge.\nSTATE: Dict[str, Any] = {}\n\n# TODO: Implement all required endpoints from the prompt.\n\n@app.get(\"/public\")\nasync def origin_allowlist_policy_get_public():\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n",
      "language": "python",
      "readonly": false
    }
  ],
  "test_config": {
    "language": "python",
    "test_requests": [
      {
        "method": "GET",
        "path": "/public?origin=https://app.local",
        "expected_status": 200,
        "expected_body": {
          "allowed": true
        }
      },
      {
        "method": "GET",
        "path": "/public?origin=https://evil.example",
        "expected_status": 403,
        "expected_body": {
          "error": "origin_blocked"
        }
      },
      {
        "method": "GET",
        "path": "/public?origin=https://admin.local",
        "expected_status": 200,
        "expected_body": {
          "allowed": true
        }
      }
    ]
  },
  "solution_files": [],
  "sort_order": 18
}
