{
  "id": "paginated-users-list",
  "title": "Paginated Users List",
  "category": "backend",
  "difficulty": "easy",
  "description": "Build offset pagination for an in-memory user collection with predictable metadata.\n\n### Objective\nImplement the FastAPI service so the replayed evaluator requests pass exactly as specified.\n\n### Implement These Routes\n- `GET /users?limit=2&offset=0`\n- `GET /users?limit=2&offset=4`\n- `GET /users?limit=3&offset=2`\n\n### Request Replay (Executed In Order)\n1. `GET /users?limit=2&offset=0`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"items\":[{\"id\":\"u1\"},{\"id\":\"u2\"}],\"next_offset\":2}`\n\n2. `GET /users?limit=2&offset=4`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"items\":[{\"id\":\"u5\"}],\"next_offset\":null}`\n\n3. `GET /users?limit=3&offset=2`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"items\":[{\"id\":\"u3\"},{\"id\":\"u4\"},{\"id\":\"u5\"}],\"next_offset\":null}`\n\n### Candidate Expectations\n- Complete the TODOs in starter code and keep behavior deterministic across sequential requests.\n- Keep state in memory for this challenge; do not use SQL or external databases.\n- Return exact status codes and JSON response shapes expected by the replay tests.\n- Handle invalid or missing input defensively with stable JSON errors instead of crashes.\n\n### Starter Files You Can Edit\n- `app/main.py`\n\n### Done Criteria\n- All replayed request tests pass in order.\n- API behavior matches the required contracts exactly.",
  "constraints": "- Python 3.11+\n- No SQL or external database usage\n- Deterministic outputs for graded requests",
  "hints": [
    "Use slicing with offset:offset+limit.",
    "Clamp limit to a reasonable max."
  ],
  "tags": [
    "backend",
    "python",
    "fastapi",
    "pagination",
    "api-design"
  ],
  "starter_files": [
    {
      "path": "app/main.py",
      "content": "from fastapi import FastAPI, HTTPException\nfrom typing import Any, Dict\n\napp = FastAPI(title=\"Paginated Users List\")\n\n# In-memory state for this challenge.\nSTATE: Dict[str, Any] = {}\n\n# TODO: Implement all required endpoints from the prompt.\n\n@app.get(\"/users\")\nasync def paginated_users_list_get_users():\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n",
      "language": "python",
      "readonly": false
    }
  ],
  "test_config": {
    "language": "python",
    "test_requests": [
      {
        "method": "GET",
        "path": "/users?limit=2&offset=0",
        "expected_status": 200,
        "expected_body": {
          "items": [
            {
              "id": "u1"
            },
            {
              "id": "u2"
            }
          ],
          "next_offset": 2
        }
      },
      {
        "method": "GET",
        "path": "/users?limit=2&offset=4",
        "expected_status": 200,
        "expected_body": {
          "items": [
            {
              "id": "u5"
            }
          ],
          "next_offset": null
        }
      },
      {
        "method": "GET",
        "path": "/users?limit=3&offset=2",
        "expected_status": 200,
        "expected_body": {
          "items": [
            {
              "id": "u3"
            },
            {
              "id": "u4"
            },
            {
              "id": "u5"
            }
          ],
          "next_offset": null
        }
      }
    ]
  },
  "solution_files": [],
  "sort_order": 2
}
