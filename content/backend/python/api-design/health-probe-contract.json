{
  "id": "health-probe-contract",
  "title": "Health Probe Contract",
  "category": "backend",
  "difficulty": "easy",
  "description": "Implement shallow and readiness health probe endpoints with a stable JSON contract for load balancers and uptime checks.\n\n### Objective\nImplement the FastAPI service so the replayed evaluator requests pass exactly as specified.\n\n### Implement These Routes\n- `GET /healthz`\n- `GET /readyz`\n\n### Request Replay (Executed In Order)\n1. `GET /healthz`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"status\":\"ok\",\"service\":\"backend-api\"}`\n\n2. `GET /readyz`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"ready\":true}`\n\n3. `GET /healthz`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"status\":\"ok\",\"service\":\"backend-api\"}`\n\n### Candidate Expectations\n- Complete the TODOs in starter code and keep behavior deterministic across sequential requests.\n- Keep state in memory for this challenge; do not use SQL or external databases.\n- Return exact status codes and JSON response shapes expected by the replay tests.\n- Handle invalid or missing input defensively with stable JSON errors instead of crashes.\n\n### Starter Files You Can Edit\n- `app/main.py`\n\n### Done Criteria\n- All replayed request tests pass in order.\n- API behavior matches the required contracts exactly.",
  "constraints": "- Python 3.11+\n- No SQL or external database usage\n- Deterministic outputs for graded requests",
  "hints": [
    "Keep probe handlers side-effect free.",
    "Return plain dict objects with stable keys."
  ],
  "tags": [
    "backend",
    "python",
    "fastapi",
    "api-contracts",
    "health-checks"
  ],
  "starter_files": [
    {
      "path": "app/main.py",
      "content": "from fastapi import FastAPI, HTTPException\nfrom typing import Any, Dict\n\napp = FastAPI(title=\"Health Probe Contract\")\n\n# In-memory state for this challenge.\nSTATE: Dict[str, Any] = {}\n\n# TODO: Implement all required endpoints from the prompt.\n\n@app.get(\"/healthz\")\nasync def health_probe_contract_get_healthz():\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n\n@app.get(\"/readyz\")\nasync def health_probe_contract_get_readyz():\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n",
      "language": "python",
      "readonly": false
    }
  ],
  "test_config": {
    "language": "python",
    "test_requests": [
      {
        "method": "GET",
        "path": "/healthz",
        "expected_status": 200,
        "expected_body": {
          "status": "ok",
          "service": "backend-api"
        }
      },
      {
        "method": "GET",
        "path": "/readyz",
        "expected_status": 200,
        "expected_body": {
          "ready": true
        }
      },
      {
        "method": "GET",
        "path": "/healthz",
        "expected_status": 200,
        "expected_body": {
          "status": "ok",
          "service": "backend-api"
        }
      }
    ]
  },
  "solution_files": [],
  "sort_order": 1
}
