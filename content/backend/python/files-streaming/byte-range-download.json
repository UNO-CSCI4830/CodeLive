{
  "id": "byte-range-download",
  "title": "Byte Range Download",
  "category": "backend",
  "difficulty": "medium",
  "description": "Implement deterministic byte-range extraction for downloadable content.\n\n### Objective\nImplement the FastAPI service so the replayed evaluator requests pass exactly as specified.\n\n### Implement These Routes\n- `GET /download-range?content=abcdef&start=1&end=3`\n- `GET /download-range?content=abc&start=2&end=1`\n- `GET /download-range?content=abcdef&start=0&end=5`\n\n### Request Replay (Executed In Order)\n1. `GET /download-range?content=abcdef&start=1&end=3`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"chunk\":\"bcd\"}`\n\n2. `GET /download-range?content=abc&start=2&end=1`\n   Request body: `(not provided)`\n   Expected status: `400`\n   Expected body: `{\"error\":\"invalid_range\"}`\n\n3. `GET /download-range?content=abcdef&start=0&end=5`\n   Request body: `(not provided)`\n   Expected status: `200`\n   Expected body: `{\"chunk\":\"abcdef\"}`\n\n### Candidate Expectations\n- Complete the TODOs in starter code and keep behavior deterministic across sequential requests.\n- Keep state in memory for this challenge; do not use SQL or external databases.\n- Return exact status codes and JSON response shapes expected by the replay tests.\n- Handle invalid or missing input defensively with stable JSON errors instead of crashes.\n\n### Starter Files You Can Edit\n- `app/main.py`\n\n### Done Criteria\n- All replayed request tests pass in order.\n- API behavior matches the required contracts exactly.",
  "constraints": "- Python 3.11+\n- No SQL or external database usage\n- Deterministic outputs for graded requests",
  "hints": [
    "Use Python slicing with end+1.",
    "Validate boundaries first."
  ],
  "tags": [
    "backend",
    "python",
    "fastapi",
    "range-requests",
    "content-serving"
  ],
  "starter_files": [
    {
      "path": "app/main.py",
      "content": "from fastapi import FastAPI, HTTPException\nfrom typing import Any, Dict\n\napp = FastAPI(title=\"Byte Range Download\")\n\n# In-memory state for this challenge.\nSTATE: Dict[str, Any] = {}\n\n# TODO: Implement all required endpoints from the prompt.\n\n@app.get(\"/download-range\")\nasync def byte_range_download_get_download_range():\n    raise HTTPException(status_code=501, detail=\"Not implemented\")\n",
      "language": "python",
      "readonly": false
    }
  ],
  "test_config": {
    "language": "python",
    "test_requests": [
      {
        "method": "GET",
        "path": "/download-range?content=abcdef&start=1&end=3",
        "expected_status": 200,
        "expected_body": {
          "chunk": "bcd"
        }
      },
      {
        "method": "GET",
        "path": "/download-range?content=abc&start=2&end=1",
        "expected_status": 400,
        "expected_body": {
          "error": "invalid_range"
        }
      },
      {
        "method": "GET",
        "path": "/download-range?content=abcdef&start=0&end=5",
        "expected_status": 200,
        "expected_body": {
          "chunk": "abcdef"
        }
      }
    ]
  },
  "solution_files": [],
  "sort_order": 35
}
