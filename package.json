{
  "name": "code-live",
  "version": "0.1.0",
  "private": true,
  "description": "Code Live — live technical interview platform monorepo",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev"
  }
}
